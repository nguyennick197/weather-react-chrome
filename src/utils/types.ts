export interface CurrentWeatherProps {
  name: string;
  temp: number;
  group: string;
  feels_like: number;
  humidity: number;
  temp_max: number;
  temp_min: number;
  wind_speed: number;
  iconId: number;
  uvi: number;
}
